# @sendflow/plugin-intent-parser

Parses Telegram-style messages into `state.values.sendflow.intent` (Solana mints, rail, receiver wallet). Uses `ModelType.OBJECT_SMALL` with **JSONSchema** only (no Zod in `useModel` params).

## Test

```bash
bun test
```
